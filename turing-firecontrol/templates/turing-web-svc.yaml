apiVersion: v1
kind: Service
metadata:
  labels:
    cattle.io/creator: norman
  name: turing-web
spec:
  ports:
  - name: 80tcp02
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app.kubernetes.io/name: turing-web
    app.kubernetes.io/instance: {{ .Release.Name }}
  type: ClusterIP
